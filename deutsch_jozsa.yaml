# Workflow API version
apiVersion: io.orquestra.workflow/1.0.0
# Prefix for workflow ID
name: deutsch-jozsa
#nameの部分を大文字にしていたらSubmitできなかったので，すべて小文字で書いたほうがいい気がする
# List components needed by workflow.
imports:
- name: deutsch-jozsa
  type: git
  parameters:
    repository: "git@github.com:TMMeme/workshop-bell-state.git"
    branch: "main"

# List the steps in the workflow
steps:
- name: run-circuit
  config:
    runtime:
      language: python3
      imports: [deutsch-jozsa] 
      parameters:
        file: deutsch-jozsa/steps/deutsch-jozsa.py
        function: main
  outputs:
  - name: results
    type: output

types:
- output